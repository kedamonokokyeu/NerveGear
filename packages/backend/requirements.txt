# --- core engine (always required) ---
numpy>=1.24

# --- server (the WebSocket control stream + CAD/RAG endpoints) ---
fastapi>=0.111
uvicorn[standard]>=0.29
websockets>=12.0          # uvicorn ws backend / client tooling
python-multipart>=0.0.9   # file uploads for /cad/upload

# --- webcam + hand tracking (only for the local-camera path) ---
# If you only ever feed landmarks from the browser (/ws/landmarks), you can skip
# these two. They are the heavy, platform-specific deps the engine avoids importing.
opencv-python>=4.9
mediapipe>=0.10.9

# --- CAD ingestion (only for /cad/upload) ---
trimesh>=4.0
scipy>=1.10               # used by trimesh voxelization (and the CFD backend)
cascadio>=0.0.14          # OpenCASCADE wrapper: STEP -> GLB for the Studio viewer

# --- RAG (optional; pipeline runs offline without these via the hashing fallback) ---
# sentence-transformers>=2.2   # uncomment for real semantic embeddings
python-docx>=1.1             # to ingest the NerveGear roadmap .docx into the RAG corpus

# --- dev / test ---
pytest>=8.0
